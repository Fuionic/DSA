// we start traveresing the string and check for 1 or 0 if 1 wee keeep increasing the count. If 0 we 
just set it to zero and after every iteration we just keep checking maxcount. These steps are done for both 0 and 1.

class Solution {
    public boolean checkZeroOnes(String s) {
        int count1 = 0;
        int maxcount1 = 0;
        int maxcount2 = 0;
        int count2 = 0;
        for(int i = 0; i<s.length(); i++){
            if(s.charAt(i) == '1'){
                count1++;
                count2 = 0;

                maxcount1 = Math.max(maxcount1, count1);
            }else{
                count2++;
                count1 = 0;
                maxcount2 = Math.max(maxcount2, count2);
            }
        }
        return (maxcount1 > maxcount2);
        
    }
}
