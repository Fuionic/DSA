// use two pointers one to travel the haystack and another to travel needle and 
check the substring i, j present.  

using indexOF() this method can also be used to do.

class Solution {
    public int strStr(String haystack, String needle) {
    
      for(int i =0 ,  j = needle.length(); j <= haystack.length();j++, i++){
        if(haystack.substring(i ,j).equals(needle))
        {   
            return i;
        }
      }
      return -1;
    }
}
